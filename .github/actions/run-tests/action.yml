name: Run Tests
description: Composite action to run tests discovered by scripts/generate-test-ci-step.mjs
runs:
  using: composite
  steps:
    - name: Run tests in test/clean-github-actions-caches.test.cjs
      id: run-clean-github-actions-caches.test
      shell: bash
      run: >-
        if [ -f "test/clean-github-actions-caches.test.cjs" ]; then
          bash bin/test-cjs --testPathPatterns="clean-github-actions-caches.test.cjs"
        else
          echo "Skipping missing test file: test/clean-github-actions-caches.test.cjs"
        fi
      env:
        NODE_ENV: test
        ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN || secrets.GITHUB_TOKEN }}
    - name: Run tests in test/filemanager/rmpath.test.mjs
      id: run-rmpath.test
      shell: bash
      run: |-
        if [ -f "test/filemanager/rmpath.test.mjs" ]; then
          bash bin/test-esm --testPathPatterns="rmpath.test.mjs"
        else
          echo "Skipping missing test file: test/filemanager/rmpath.test.mjs"
        fi
      env:
        NODE_ENV: test
        ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN || secrets.GITHUB_TOKEN }}
    - name: Run tests in test/find-node-modules.test.ts
      id: run-find-node-modules.test
      shell: bash
      run: |-
        if [ -f "test/find-node-modules.test.ts" ]; then
          npm test -- --testPathPatterns="find-node-modules.test.ts"
        else
          echo "Skipping missing test file: test/find-node-modules.test.ts"
        fi
      env:
        NODE_ENV: test
        ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN || secrets.GITHUB_TOKEN }}
    - name: Run tests in test/getFileTreeString.test.cjs
      id: run-getFileTreeString.test
      shell: bash
      run: |-
        if [ -f "test/getFileTreeString.test.cjs" ]; then
          bash bin/test-cjs --testPathPatterns="getFileTreeString.test.cjs"
        else
          echo "Skipping missing test file: test/getFileTreeString.test.cjs"
        fi
      env:
        NODE_ENV: test
        ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN || secrets.GITHUB_TOKEN }}
    - name: Run tests in test/getLatestCommit.test.mjs
      id: run-getLatestCommit.test
      shell: bash
      run: |-
        if [ -f "test/getLatestCommit.test.mjs" ]; then
          bash bin/test-esm --testPathPatterns="getLatestCommit.test.mjs"
        else
          echo "Skipping missing test file: test/getLatestCommit.test.mjs"
        fi
      env:
        NODE_ENV: test
        ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN || secrets.GITHUB_TOKEN }}
    - name: Run tests in test/git-diff.test.cjs
      id: run-git-diff.test
      shell: bash
      run: |-
        if [ -f "test/git-diff.test.cjs" ]; then
          bash bin/test-cjs --testPathPatterns="git-diff.test.cjs"
        else
          echo "Skipping missing test file: test/git-diff.test.cjs"
        fi
      env:
        NODE_ENV: test
        ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN || secrets.GITHUB_TOKEN }}
    - name: Run tests in test/git-fix-focused.test.cjs
      id: run-git-fix-focused.test
      shell: bash
      run: |-
        if [ -f "test/git-fix-focused.test.cjs" ]; then
          bash bin/test-cjs --testPathPatterns="git-fix-focused.test.cjs"
        else
          echo "Skipping missing test file: test/git-fix-focused.test.cjs"
        fi
      env:
        NODE_ENV: test
        ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN || secrets.GITHUB_TOKEN }}
    - name: Run tests in test/git-fix.help.test.cjs
      id: run-git-fix.help.test
      shell: bash
      run: |-
        if [ -f "test/git-fix.help.test.cjs" ]; then
          bash bin/test-cjs --testPathPatterns="git-fix.help.test.cjs"
        else
          echo "Skipping missing test file: test/git-fix.help.test.cjs"
        fi
      env:
        NODE_ENV: test
        ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN || secrets.GITHUB_TOKEN }}
    - name: Run tests in test/git-fix.options.test.cjs
      id: run-git-fix.options.test
      shell: bash
      run: |-
        if [ -f "test/git-fix.options.test.cjs" ]; then
          bash bin/test-cjs --testPathPatterns="git-fix.options.test.cjs"
        else
          echo "Skipping missing test file: test/git-fix.options.test.cjs"
        fi
      env:
        NODE_ENV: test
        ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN || secrets.GITHUB_TOKEN }}
    - name: Run tests in test/git-fix.summary.test.cjs
      id: run-git-fix.summary.test
      shell: bash
      run: |-
        if [ -f "test/git-fix.summary.test.cjs" ]; then
          bash bin/test-cjs --testPathPatterns="git-fix.summary.test.cjs"
        else
          echo "Skipping missing test file: test/git-fix.summary.test.cjs"
        fi
      env:
        NODE_ENV: test
        ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN || secrets.GITHUB_TOKEN }}
    - name: Run tests in test/git-user-config.test.cjs
      id: run-git-user-config.test
      shell: bash
      run: |-
        if [ -f "test/git-user-config.test.cjs" ]; then
          bash bin/test-cjs --testPathPatterns="git-user-config.test.cjs"
        else
          echo "Skipping missing test file: test/git-user-config.test.cjs"
        fi
      env:
        NODE_ENV: test
        ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN || secrets.GITHUB_TOKEN }}
    - name: Run tests in test/git-utils.test.cjs
      id: run-git-utils.test
      shell: bash
      run: |-
        if [ -f "test/git-utils.test.cjs" ]; then
          bash bin/test-cjs --testPathPatterns="git-utils.test.cjs"
        else
          echo "Skipping missing test file: test/git-utils.test.cjs"
        fi
      env:
        NODE_ENV: test
        ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN || secrets.GITHUB_TOKEN }}
    - name: Run tests in test/gitattributes-parse.test.cjs
      id: run-gitattributes-parse.test
      shell: bash
      run: |-
        if [ -f "test/gitattributes-parse.test.cjs" ]; then
          bash bin/test-cjs --testPathPatterns="gitattributes-parse.test.cjs"
        else
          echo "Skipping missing test file: test/gitattributes-parse.test.cjs"
        fi
      env:
        NODE_ENV: test
        ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN || secrets.GITHUB_TOKEN }}
    - name: Run tests in test/gitattributes-update.test.cjs
      id: run-gitattributes-update.test
      shell: bash
      run: |-
        if [ -f "test/gitattributes-update.test.cjs" ]; then
          bash bin/test-cjs --testPathPatterns="gitattributes-update.test.cjs"
        else
          echo "Skipping missing test file: test/gitattributes-update.test.cjs"
        fi
      env:
        NODE_ENV: test
        ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN || secrets.GITHUB_TOKEN }}
    - name: Run tests in test/gitattributes.test.cjs
      id: run-gitattributes.test
      shell: bash
      run: |-
        if [ -f "test/gitattributes.test.cjs" ]; then
          bash bin/test-cjs --testPathPatterns="gitattributes.test.cjs"
        else
          echo "Skipping missing test file: test/gitattributes.test.cjs"
        fi
      env:
        NODE_ENV: test
        ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN || secrets.GITHUB_TOKEN }}
    - name: Run tests in test/node-executor.test.cjs
      id: run-node-executor.test
      shell: bash
      run: |-
        if [ -f "test/node-executor.test.cjs" ]; then
          bash bin/test-cjs --testPathPatterns="node-executor.test.cjs"
        else
          echo "Skipping missing test file: test/node-executor.test.cjs"
        fi
      env:
        NODE_ENV: test
        ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN || secrets.GITHUB_TOKEN }}
    - name: Run tests in test/package-resolutions-updater.test.cjs
      id: run-package-resolutions-updater.test
      shell: bash
      run: >-
        if [ -f "test/package-resolutions-updater.test.cjs" ]; then
          bash bin/test-cjs --testPathPatterns="package-resolutions-updater.test.cjs"
        else
          echo "Skipping missing test file: test/package-resolutions-updater.test.cjs"
        fi
      env:
        NODE_ENV: test
        ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN || secrets.GITHUB_TOKEN }}
    - name: Run tests in test/readline-mock.test.ts
      id: run-readline-mock.test
      shell: bash
      run: |-
        if [ -f "test/readline-mock.test.ts" ]; then
          npm test -- --testPathPatterns="readline-mock.test.ts"
        else
          echo "Skipping missing test file: test/readline-mock.test.ts"
        fi
      env:
        NODE_ENV: test
        ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN || secrets.GITHUB_TOKEN }}
    - name: Run tests in test/remove-module.test.ts
      id: run-remove-module.test
      shell: bash
      run: |-
        if [ -f "test/remove-module.test.ts" ]; then
          npm test -- --testPathPatterns="remove-module.test.ts"
        else
          echo "Skipping missing test file: test/remove-module.test.ts"
        fi
      env:
        NODE_ENV: test
        ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN || secrets.GITHUB_TOKEN }}
    - name: Run tests in test/replaceRawWithLatestHash.test.mjs
      id: run-replaceRawWithLatestHash.test
      shell: bash
      run: >-
        if [ -f "test/replaceRawWithLatestHash.test.mjs" ]; then
          bash bin/test-esm --testPathPatterns="replaceRawWithLatestHash.test.mjs"
        else
          echo "Skipping missing test file: test/replaceRawWithLatestHash.test.mjs"
        fi
      env:
        NODE_ENV: test
        ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN || secrets.GITHUB_TOKEN }}
    - name: Run tests in test/resolvePackageResolutionUpdates.test.mjs
      id: run-resolvePackageResolutionUpdates.test
      shell: bash
      run: >-
        if [ -f "test/resolvePackageResolutionUpdates.test.mjs" ]; then
          bash bin/test-esm --testPathPatterns="resolvePackageResolutionUpdates.test.mjs"
        else
          echo "Skipping missing test file: test/resolvePackageResolutionUpdates.test.mjs"
        fi
      env:
        NODE_ENV: test
        ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN || secrets.GITHUB_TOKEN }}
    - name: Run tests in test/rm-node-modules.test.cjs
      id: run-rm-node-modules.test
      shell: bash
      run: |-
        if [ -f "test/rm-node-modules.test.cjs" ]; then
          bash bin/test-cjs --testPathPatterns="rm-node-modules.test.cjs"
        else
          echo "Skipping missing test file: test/rm-node-modules.test.cjs"
        fi
      env:
        NODE_ENV: test
        ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN || secrets.GITHUB_TOKEN }}
    - name: Run tests in test/run-by-checksum/runChecksum-cli.spawn.test.mjs
      id: run-runChecksum-cli.spawn.test
      shell: bash
      run: >-
        if [ -f "test/run-by-checksum/runChecksum-cli.spawn.test.mjs" ]; then
          bash bin/test-esm --testPathPatterns="runChecksum-cli.spawn.test.mjs"
        else
          echo "Skipping missing test file: test/run-by-checksum/runChecksum-cli.spawn.test.mjs"
        fi
      env:
        NODE_ENV: test
        ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN || secrets.GITHUB_TOKEN }}
    - name: Run tests in test/run-by-checksum/runChecksum-cli.test.mjs
      id: run-runChecksum-cli.test
      shell: bash
      run: >-
        if [ -f "test/run-by-checksum/runChecksum-cli.test.mjs" ]; then
          bash bin/test-esm --testPathPatterns="runChecksum-cli.test.mjs"
        else
          echo "Skipping missing test file: test/run-by-checksum/runChecksum-cli.test.mjs"
        fi
      env:
        NODE_ENV: test
        ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN || secrets.GITHUB_TOKEN }}
    - name: Run tests in test/run-by-checksum/runChecksum-dry-run.test.mjs
      id: run-runChecksum-dry-run.test
      shell: bash
      run: >-
        if [ -f "test/run-by-checksum/runChecksum-dry-run.test.mjs" ]; then
          bash bin/test-esm --testPathPatterns="runChecksum-dry-run.test.mjs"
        else
          echo "Skipping missing test file: test/run-by-checksum/runChecksum-dry-run.test.mjs"
        fi
      env:
        NODE_ENV: test
        ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN || secrets.GITHUB_TOKEN }}
    - name: Run tests in test/run-by-checksum/runChecksum.test.mjs
      id: run-runChecksum.test
      shell: bash
      run: >-
        if [ -f "test/run-by-checksum/runChecksum.test.mjs" ]; then
          bash bin/test-esm --testPathPatterns="runChecksum.test.mjs"
        else
          echo "Skipping missing test file: test/run-by-checksum/runChecksum.test.mjs"
        fi
      env:
        NODE_ENV: test
        ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN || secrets.GITHUB_TOKEN }}
    - name: Run tests in test/submodule-install.test.cjs
      id: run-submodule-install.test
      shell: bash
      run: |-
        if [ -f "test/submodule-install.test.cjs" ]; then
          bash bin/test-cjs --testPathPatterns="submodule-install.test.cjs"
        else
          echo "Skipping missing test file: test/submodule-install.test.cjs"
        fi
      env:
        NODE_ENV: test
        ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN || secrets.GITHUB_TOKEN }}
    - name: Run tests in test/utils/fetchResponse.test.mjs
      id: run-fetchResponse.test
      shell: bash
      run: |-
        if [ -f "test/utils/fetchResponse.test.mjs" ]; then
          bash bin/test-esm --testPathPatterns="fetchResponse.test.mjs"
        else
          echo "Skipping missing test file: test/utils/fetchResponse.test.mjs"
        fi
      env:
        NODE_ENV: test
        ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN || secrets.GITHUB_TOKEN }}
    - name: Run tests in test/utils/isGitRepository.test.cjs
      id: run-isGitRepository.test
      shell: bash
      run: |-
        if [ -f "test/utils/isGitRepository.test.cjs" ]; then
          bash bin/test-cjs --testPathPatterns="isGitRepository.test.cjs"
        else
          echo "Skipping missing test file: test/utils/isGitRepository.test.cjs"
        fi
      env:
        NODE_ENV: test
        ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN || secrets.GITHUB_TOKEN }}
    - name: Run tests in test/utils/isGitRepository.test.js
      id: run-isGitRepository.test
      shell: bash
      run: |-
        if [ -f "test/utils/isGitRepository.test.js" ]; then
          npm test -- --testPathPatterns="isGitRepository.test.js"
        else
          echo "Skipping missing test file: test/utils/isGitRepository.test.js"
        fi
      env:
        NODE_ENV: test
        ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN || secrets.GITHUB_TOKEN }}
    - name: Run tests in test/validate-tarball-npm.test.cjs
      id: run-validate-tarball-npm.test
      shell: bash
      run: |-
        if [ -f "test/validate-tarball-npm.test.cjs" ]; then
          bash bin/test-cjs --testPathPatterns="validate-tarball-npm.test.cjs"
        else
          echo "Skipping missing test file: test/validate-tarball-npm.test.cjs"
        fi
      env:
        NODE_ENV: test
        ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN || secrets.GITHUB_TOKEN }}
    - name: Run tests in test/validate-tarball-yarn.test.cjs
      id: run-validate-tarball-yarn.test
      shell: bash
      run: |-
        if [ -f "test/validate-tarball-yarn.test.cjs" ]; then
          bash bin/test-cjs --testPathPatterns="validate-tarball-yarn.test.cjs"
        else
          echo "Skipping missing test file: test/validate-tarball-yarn.test.cjs"
        fi
      env:
        NODE_ENV: test
        ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN || secrets.GITHUB_TOKEN }}
