extract context based on session title "New session"

```bash
python -u "d:\Repositories\binary-collections\src\opencode\opencode_context_explorer.py" --db "C:\Users\Dell\.local\share\opencode" export --all --search "New session" --limit 1000
```